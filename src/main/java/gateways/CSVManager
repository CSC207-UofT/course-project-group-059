package gateways;

import storer.Storable;

import java.io.File;

public class CSVManager {
    //types used for read()
    public static final String TASKLIST = "tasklist";
    public static final String TIMELINE = "timeline";

    public void read(File f, String type) {
        //calls open() on file based on type given
    }

    public void write(Storable s) {
        //gets return value of s.save() and writes it into a .csv file
    }
}